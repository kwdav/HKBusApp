# Firebase Configuration
# Copy this file to .env and fill in your actual values

# Path to Firebase service account key JSON file (absolute path recommended)
# Download from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_PATH=/path/to/your/firebase-service-account-key.json

# Firebase Storage bucket name (format: project-id.appspot.com)
# Find at: Firebase Console → Storage (shown at top of page)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Output directory for generated JSON file (absolute path recommended for cron jobs)
# Default: same directory as script if not specified
OUTPUT_DIRECTORY=/share/scripts/hkbus/output

# Log directory (absolute path recommended)
# Default: ./logs if not specified
LOG_DIRECTORY=/share/scripts/hkbus/logs
